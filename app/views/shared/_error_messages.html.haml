- if object.errors.any?
  #error_explanation
    .alert.alert-danger
      = "#{pluralize(object.errors.count, 'error')} detected:"
    %ul
      - object.errors.full_messages.each do |msg|
        %li
          = msg
